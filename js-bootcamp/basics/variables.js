// 1. you cant define a variable more than once
let petName = 'Rosa';

petName = 'Stinky';

console.log(petName);

//2. there are rules related to variable names

let test$_ = 2;

let result = 3 + 4;

// 3. variables names cannot be reseverd keywords

